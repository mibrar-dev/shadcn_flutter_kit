{
  "$schema": "../../../manifests/readme_meta.schema.json",
  "schemaVersion": 1,
  "id": "refresh_trigger",
  "name": "Refresh Trigger",
  "description": "Pull-to-refresh wrapper with customizable indicator, extents, and completion animations.",
  "install": "flutter_shadcn add refresh_trigger",
  "import": "import 'package:<your_app>/ui/shadcn/overlay/refresh_trigger/refresh_trigger.dart';",
  "whenToUse": {
    "use": [
      "you need a pull-to-refresh interaction on scrollables.",
      "you want custom indicator visuals."
    ],
    "avoid": [
      "you already use platform-specific refresh controls."
    ]
  },
  "examples": {
    "minimal": "RefreshTrigger(\n  onRefresh: () async {\n    await fetchLatest();\n  },\n  child: ListView(\n    children: const [Text('Item')],\n  ),\n)"
  },
  "patterns": [
    {
      "title": "Custom indicator",
      "code": "RefreshTrigger(\n  indicatorBuilder: (context, stage) => DefaultRefreshIndicator(stage: stage),\n  onRefresh: () async {},\n  child: ListView(children: const [Text('Item')]),\n)"
    }
  ],
  "api": {
    "constructors": [],
    "callbacks": [],
    "types": [
      "`RefreshTrigger`",
      "`RefreshTriggerStage`",
      "`RefreshIndicatorBuilder`",
      "`RefreshTriggerTheme`"
    ]
  },
  "theming": [
    "`RefreshTriggerTheme` controls min/max extents, curves, and completion duration."
  ],
  "accessibility": [
    "Ensure the refresh action also has a non-gesture alternative (button/shortcut)."
  ],
  "do": [
    "Keep `minExtent` reachable without excessive pulling."
  ],
  "dont": [
    "Trigger long-running refresh operations without feedback."
  ],
  "related": [
    "scrollview",
    "spinner"
  ],
  "registryRules": [
    "One public class per file",
    "Helpers under `_impl/`"
  ]
}
